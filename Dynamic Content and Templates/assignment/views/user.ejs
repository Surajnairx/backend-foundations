<%-include('includes/header.ejs')%>
<%if(users.length > 0){%>
  <h1>User Found</h1>
  <ul>
    <%users.forEach((user)=>{%>
    <li><%=user.name%></li>
    <%})%>
  </ul>

<%}else{%>
  <h1>No User Found</h1>
  <%}%>

  <%-include('includes/end.ejs')>